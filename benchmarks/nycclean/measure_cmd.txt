#!/bin/bash
./bench -f data/taxi_sf\=1.csv.small.0.5 -p loop-fusion unroll-static-loop infer-size inline-literals unroll-structs inline-let inline-let-getfield measurement inline-let inline-let-getfield vectorize short-circuit-booleans -s 1 -t 1
